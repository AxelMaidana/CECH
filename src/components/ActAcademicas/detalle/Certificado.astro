--- 
import Certificadoo from '../../modales/Certificado';

const { postId, postTitulo } = Astro.props;
---

<div class="flex flex-col md:flex-row py-10 px-6 md:px-10 lg:px-20 xl:px-40 justify-between items-center bg-[url('/media/certificado.png')] bg-cover bg-center bg-no-repeat relative">
    <div class="absolute inset-0 bg-gradient-to-t from-white via-transparent to-white pointer-events-none"></div>

    <div class="relative z-10">
        <h1 class="text-center md:text-start text-3xl md:text-3xl font-extrabold md:max-w-md text-black/85">¿Te gustaría obtener tu Certificado?</h1>
        <p class="text-center md:text-start text-gray-700 font-semibold mt-2 max-w-md md:max-w-xs lg:max-w-md pt-2">
            Si estás interesado en recibir tu certificado, haz clic en el botón de abajo y lo gestionaremos para vos.
        </p>
    </div>
    
    <div class="relative z-10 mt-8 | md:mt-0 | lg:p-0">
            <Certificadoo client:only="react" postId={postId} postTitulo={postTitulo} />
    </div>

</div>
